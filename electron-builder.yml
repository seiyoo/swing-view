appId: dev.seiyoo.swing-view
productName: SwingView
directories:
  buildResources: build
  output: dist
files:
  - out/**
  - package.json
win:
  target:
    - nsis
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  artifactName: ${productName}-${version}-setup.${ext}
